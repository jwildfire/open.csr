{
  "component": "quality",
  "matrix": "quality.md",
  "requirements": {
    "QC-MATRIX-001": "A requirement matrix row contributes to the extract only when its ID cell is exactly a requirement ID matching `^[A-Z]{2,4}-[A-Z]+-\\d+[A-D]?$` and its requirement text is non-empty.",
    "QC-MATRIX-002": "When a table header row is present, columns are located by name (ID, Area, Requirement, Source, Evidence Type, Status); the safety.viz positional layout is the fallback.",
    "QC-MATRIX-003": "Requirement text containing an escaped pipe (`|`) is extracted intact rather than split into two cells.",
    "QC-MATRIX-004": "Tables in the document that are not requirement tables, and prose containing requirement IDs, contribute nothing to the extract.",
    "QC-MATRIX-005": "The committed extract is timestamp-free and a pure function of the matrix content, so the freshness guard is a plain content comparison.",
    "QC-DRIFT-001": "`requirements --check` reports added, removed and text-changed requirement IDs and exits non-zero when the committed extract differs from a fresh extraction.",
    "QC-DRIFT-002": "`evidence --check` reports missing tests, new tests and pass/fail changes, and ignores provenance and traceability fields entirely.",
    "QC-NAME-001": "Every test title in both suites matches `\"<REQ-ID>[, <REQ-ID>]: <description> (#<issue>)\"`; a guard test in each suite enforces it.",
    "QC-NAME-002": "Requirement IDs and issue references are parsed out of a test title, supporting several IDs and several issue refs in one title.",
    "QC-EVID-001": "Vitest JSON reporter output normalizes into contracts §8 records carrying `requirementIds`, `title`, `suite`, `passed` and `file`.",
    "QC-EVID-002": "testthat results from `qc/testthat-results.json` normalize into the same record shape with `suite: \"r-unit\"`.",
    "QC-EVID-003": "Any testthat status that is not exactly `pass` (fail, error, skip) records as not passed, so the evidence page never overstates a run.",
    "QC-EVID-004": "Human review of a text block records as a `text-review` suite record carrying `reviewedBy` and `reviewedAt`, so one page shows automated and human evidence together.",
    "QC-ROUTE-001": "A JS test file routes to its module by path — `tests/unit/<module>/**` or `tests/unit/<module>-<topic>.test.js`.",
    "QC-ROUTE-002": "An R test file `test-<module>[-<topic>].R` routes to `<module>` by longest-prefix match against the registry.",
    "QC-ROUTE-003": "A test that routes to no module is shared scaffold and is duplicated into every module's evidence set, so each set is self-contained.",
    "QC-ROUTE-004": "The module universe comes from `site/config.json` — components plus displays — so registering a display is all it takes for its evidence set to appear.",
    "QC-ROUTE-005": "A component whose test files are named by topic rather than by module declares its file stems in the registry (`testPrefixes`), and the longest declared prefix wins.",
    "QC-ROUTE-006": "A whole suite may be owned by one component through the registry (`suites`), which is how `text-review` records route to the Text Library regardless of file path.",
    "QC-ROUTE-007": "When a test file covers several modules, records route by the requirement-ID prefixes their owning registry entries declare; a record whose IDs no entry claims stays shared.",
    "QC-EVID-005": "A text block awaiting human approval is reported as pending review and blocks assembly, but does not fail the evidence run — draft is a lifecycle state, not a defect.",
    "QC-EVID-006": "Test-file paths in a committed evidence set are recorded relative to the repository root, including when the run happens in a git worktree, so the artifact names the repository rather than the machine that produced it.",
    "QC-UNRES-001": "Requirement IDs referenced by a test but defined in no matrix are reported as UNRESOLVED at build time and rendered on the Quality page.",
    "QC-UNRES-002": "Reviewed requirements that no test references are reported as uncovered, with a coverage proportion per component.",
    "QC-UNRES-003": "An ID defined in a sibling component's matrix counts as resolved, not unresolved, so shared IDs do not produce false alarms.",
    "QC-SITE-001": "The shared shell substitutes title, description, navigation and root prefix, so one build serves the site at any mount depth via relative URLs.",
    "QC-SITE-002": "The build fails when any internal `href`/`src` in the emitted HTML does not resolve to an emitted file.",
    "QC-SITE-003": "The build fails when the emitted site references any external resource — script, stylesheet, font, image or CSS import — so the whole site works offline.",
    "QC-SITE-004": "HTML embedded from the pipeline (a rendered display, an assembled fragment) is stripped of scripts, event handlers, external stylesheets and document wrappers before it is placed on a page.",
    "QC-SITE-005": "A registry entry's status is derived from the filesystem — `planned` until an ARD exists, `built` once it does, `evidenced` once an evidence set does.",
    "QC-SITE-006": "Every page renders a documented \"not yet generated\" state when its input is absent, and the build never throws on a missing or malformed input.",
    "QC-SITE-007": "A display directory present on disk but absent from the registry renders as an unregistered card rather than disappearing from the gallery.",
    "QC-CI-001": "CI runs the JS suite, both freshness guards and the site build on every pull request, and runs the R suite in a separate job.",
    "QC-CI-002": "The built site publishes to GitHub Pages from the default branch through `actions/deploy-pages` under the `pages` concurrency group.",
    "QC-DEMO-001": "The Demo app's selection — active tab, display, text block, focus target — round-trips losslessly through the URL hash, so any view of the report is a shareable link.",
    "QC-DEMO-002": "An unknown or absent tab in a deep link decodes to the default tab; the four tabs are Reader, Tables, Text and Templates.",
    "QC-DEMO-003": "A link from one pane to another pane's standalone surface resolves to a tab switch carrying the selection it named — a display permalink selects that display, a text-block anchor selects that block — at any authored path depth.",
    "QC-DEMO-004": "Links that are not pane crossings are left to navigate: Quality, Design & Research, absolute URLs, and bare fragments (which are in-pane scrolling).",
    "QC-DEMO-005": "A crossing that names no display keeps the display already selected, so moving between panes does not silently reset what the visitor was looking at.",
    "QC-DEMO-006": "A display slug that is not in the build falls back to the first available display rather than rendering a blank pane.",
    "QC-DEMO-007": "Every pane is server-rendered and present in the emitted HTML with exactly one visible; the app's tablist uses its own class vocabulary and carries the tablist/tab/tabpanel ARIA wiring.",
    "QC-DEMO-008": "A tab whose pane did not render is dropped from the tablist rather than shown empty.",
    "QC-DEMO-009": "The Tables pane renders one panel per display and one picker entry per display, shows exactly one, opens on the requested display, and falls back to the first when the request is unknown.",
    "QC-DEMO-010": "An empty display library renders the documented empty state rather than an app with no content.",
    "QC-DEMO-011": "The Templates pane renders every section of the E3 model, marking those this report does not populate, rather than trimming the skeleton to what happens to be built.",
    "QC-DEMO-012": "Display numbers in the Templates pane are derived from assembly order by the same tested function the assembler uses, never authored; reordering the assembly moves every number and changes no slug.",
    "QC-DEMO-013": "The Templates pane renders its documented empty state when the section model is missing, and renders correctly on a tree where the assembler has never run.",
    "QC-DEMO-014": "The auto-generated provenance section is identified as generated in the Templates pane.",
    "QC-DEMO-016": "The application strip's four views are real links to real pages, marked with `aria-current` rather than tab-selection state, so the bar is working navigation with no JavaScript and the client only upgrades it.",
    "QC-DEMO-017": "The application strip carries a live context readout — study, assigned display number, slug, iteration and short ARD hash — built from an embedded index that is escaped so it cannot close its own script element.",
    "QC-DEMO-018": "The navigation tree is rooted at the study, with documents, displays and text as the collections beneath it; more than one document is listed, and one that is not built is marked rather than hidden.",
    "QC-DEMO-019": "A display records every document in the library that places it — not only the primary one — and the explorer states no display number at all; a text block that is not on disk is left out of the tree while a draft block is flagged in it.",
    "QC-DEMO-020": "The explorer renders the study, every group with its item count, opens only the active group, and marks the selected item.",
    "QC-DEMO-021": "Every selectable item in the explorer is a real link carrying its own deep link, and an unbuilt item is not a link at all.",
    "QC-DEMO-022": "The header carries a Read/Edit mode control in which Read is current and Edit is genuinely disabled, and the explorer renders without throwing on an empty registry.",
    "QC-DEMO-023": "A built document contributes its own top-level sections to the tree, and a section counts as populated when it or anything beneath it is; an unbuilt document contributes none.",
    "QC-DEMO-024": "Sections render beneath their own document, each a deep link to its heading, with unpopulated sections still navigable and labelled as modelled-but-not-populated.",
    "QC-DEMO-025": "Expansion and selection are one state, not two: the open document shows its table of contents and every other document shows its title alone, with no per-document disclosure control in the markup, no per-document collapse flag in the client, and nothing in the stylesheet that can hide the open document's contents. Group-level collapse is unaffected.",
    "QC-DEMO-026": "Sections the report does not populate are dimmed as a whole row and keep the tooltip that explains why, while populated sections stay at full contrast; a group with no items renders as a heading rather than an empty folder.",
    "QC-DEMO-027": "The Values pane shows every value's name, number and provenance — an ARD address with its iteration and hash, or the declared arithmetic for a derived value — and the text blocks that cite it.",
    "QC-DEMO-028": "The Values pane states the verdict of the gate that re-derived the store against the committed ARDs, and renders a documented empty state when no store exists.",
    "QC-DEMO-029": "Values are a collection of the study in the navigation tree, listed with their formatted numbers, and selecting one focuses it in the pane rather than introducing a fourth selection key.",
    "QC-DEMO-030": "A link to any document's standalone reader page resolves to the Documents view naming that document — the primary document's page by the key `index`, every other template object by its own — so a second document is absorbed into the app the way a display permalink already is.",
    "QC-DEMO-031": "A document selection resolves against the documents the page actually rendered, accepting either a document id or the basename of that document's reader page, and falls back to the first rendered document rather than to an empty pane.",
    "QC-DEMO-032": "The Documents pane renders one panel per assembled document from the same reader renderer, keyed by both document id and reader-page basename, shows exactly one, and hoists a single trace panel and index above the set rather than emitting one per document.",
    "QC-DEMO-033": "A document the page rendered is a selectable explorer node carrying its standalone permalink as the no-JavaScript fallback; a document the page did not render stays an ordinary link out with no selection attributes.",
    "QC-DEMO-034": "Every rendered document contributes its own top-level sections to the explorer, each section link naming its document, and a section slug is resolved inside the document on screen rather than across the page.",
    "QC-DEMO-035": "Each document panel carries its own prose disclosure, so a document whose blocks are unreviewed reads as draft inside the app exactly as it does on its own page.",
    "QC-SITE-013": "The display store states which documents place each display, built over the whole template library: the display's own page lists every document with a link to it and the number that document assigned, a display no document places says so, and the gallery card names those documents rather than counting them.",
    "QC-SITE-014": "A display placed in a document carries a hyperlink to its entry in the display store, written in the form the app's link-interception rule recognises, so the same href is a pane switch inside the demo and a real navigation on the standalone reader page.",
    "QC-SITE-015": "A display's page lists the requirements that cover that display — the matrix rows matching the prefixes it declares in `site/config.json`, together with the ids its own evidence cites — and not the whole matrix. A display declaring no prefixes lists none rather than all of them.",
    "QC-SITE-012": "The stylesheet expresses the explorer's one state and only that: a document's section list is hidden by default and revealed by sitting next to the current document, with no per-node collapse class and no disclosure-control rules left to override it.",
    "QC-SITE-011": "Two documents assembled from the shared libraries quote the same artifacts: every display both documents place resolves to the same ARD source and the same ARD payload, and both carry the same value store with no value differing in raw value or formatted string. Only the per-document display number differs.",
    "QC-SITE-008": "`site/site.css` parses as a whole: every block is closed, no block is closed that was never opened, and no declaration sits outside a rule — so no rule in the file can be silently dead in the browser.",
    "QC-SITE-009": "The site publishes one reader page and one template page for every assembled template object in `library/templates/`, discovered from the library rather than declared: an object absent from `site/config.json` still publishes, titled from its own model, and every document reaches every other from the switcher, the navigation tree and the home page.",
    "QC-SITE-010": "Every document states whether its own prose has been reviewed, derived from the `approval.state` the assembler recorded on each block it placed: a document holding unapproved blocks carries a notice above its first section and a flag in the switcher, the navigation tree and its home-page card; a document whose blocks are all approved says that instead.",
    "QC-DATA-001": "The site reads a data registry (`site/config.json` → `data`) together with the vendored package's `PROVENANCE.json`: every registered dataset carries its upstream path, blob SHA-1, SHA-256 and byte counts and the package its pinned commit and licence; a registered dataset with no vendored file is planned, a derived or alternate-lane dataset is not reported as a missing file, and a vendored file nobody registered is published with a warning, the way an unregistered display is. A repository with no registry renders the empty state.",
    "QC-DATA-002": "The reverse index is built from the ARDs alone: a display whose current ARD names a dataset in its provenance envelope appears under that dataset with the iteration, row count, prepared hash and ARD hash the envelope recorded; a dataset no ARD names has an empty entry; a dataset an ARD names that the registry does not know still gets one.",
    "QC-DATA-003": "The Data pane is one page with the package record, one section per dataset (facts, derivation notes, the read-by table linking each display in the form the app absorbs, the upstream path linking the source repository at the pinned commit, a derived dataset linking what it was derived from) and the lanes section naming both packagings and listing only the variables the agreement record says differ; each dataset also has a standalone page with the same facts.",
    "QC-DATA-004": "Given the data index, a display's header links each dataset it declares to that dataset's page, carrying as its title the row count and hash THIS iteration's ARD recorded and a short hash beside the link; a dataset the header names but the ARD does not is still linked and says so; without the index the labels print as before.",
    "QC-DATA-005": "Data is a view of the study after Values and a collection in the explorer with one item per dataset (its id as the number, planned when not vendored) plus the lanes; a dataset permalink (`data/<id>.html`) is absorbed into the Data view with that dataset in focus, the lanes and the index likewise, at any authored depth.",
    "QC-META-001": "The study page renders `library/study.yaml` as declared: id, title, phase, cut-off and source, the arms in print order, the columns that carry an arm label with the assignment each means, and every analysis set with its label, flag and subjects per arm totalled — linking to the Data section for the source; a repository without a study model renders the empty state and reports it in the build warnings.",
    "QC-META-002": "The specifications page lists every display with its analysis and display files, its current iteration's spec, display and ARD hashes, and every iteration with the actor, change request and hashes its manifest recorded (either manifest shape); custom code shared through `custom_from` is stated from the borrowing side and the owning side; value declarations are read from `library/values/values.yaml` with their kind and declaration, each linking to the store.",
    "QC-META-003": "The approvals page lists every text block with its tier, version, approval state, approver, date and drafting model from its frontmatter, with counts of approved, draft and generated blocks held out of the report, each block linking to the Text view.",
    "QC-META-004": "Environments are every distinct R, operating-system and package-version record across every iteration of every display, each listing the iterations built in it; document models are the template objects on disk with sections, slots and the documents assembled from each, an empty library saying so; the requirements page lists every matrix under `quality/requirements/` with its row count, its source link and a route to its evidence page.",
    "QC-META-005": "Metadata is a view of the study after Data and a collection in the explorer with one item per page (the study id, the counts as numbers); the pane carries every section by id; a page permalink (`metadata/<page>.html`) is absorbed into the view with that section in focus and a fragment on it selects the deeper anchor; the Templates view keeps its own tab and permalink as the shortcut to the document models.",
    "QC-LINK-001": "Given the links, a display's header names the analysis set as the study model defines it (label and flag) and the grouping column, the specification history (iteration count), the environment (R version), the text blocks that bind it and the values sourced from it — each a link into the view that owns it; without the links the header prints as before; an empty list says so rather than printing nothing.",
    "QC-LINK-002": "The assembler records what a document was built from (`inputs`: the template object, the study model, every placed display with its iteration, ARD hash and datasets, every block with its state and derived inputs, every value cited, every dataset reached with the displays that read it) and, per block, the displays its bindings resolved against with iteration and hash; the Reader renders that record as a panel of links above the text and the provenance appendix as links to each display, each dataset and the environment.",
    "QC-LINK-003": "A text block's card lists its inputs derived from its bindings — each display bound with the iteration and ARD hash resolved against, the values cited, its section and its approval, each a link — and names a declared display the block binds nothing from as drift; the frontmatter's `displays:` list stays the author's declaration, the build already failing a block that binds an undeclared display.",
    "QC-LINK-004": "The values pane links the arm a source address names to the study model, the datasets behind the value's display to the Data section, and the documents that place the blocks citing it; the arm link needs no index and is always present, the data and document links appear only when their indexes are given.",
    "QC-LINK-005": "A document whose build failed a gate is flagged in the explorer (`gates fail`), one that passed is not, and one not yet assembled carries no verdict.",
    "QC-PIPE-001": "The registry (`site/config.json` → `pipeline.functions`) declares the pipeline's functions by the code's own names with kind, code path, what each reads and writes, what it calls and its gates; the site builds one page per function stating those, linking the code, and listing every element the function produced — datasets read, displays with a current iteration, values in the store, documents assembled — each a link back; a call to an unregistered function is a build warning.",
    "QC-PIPE-002": "The explorer arranges its groups in three parts in the pipeline's order — Inputs (Data, Metadata, Text), Pipeline (the functions), Outputs (Displays, Values, Documents) — each part headed and noted, while `groups` stays the flat list every consumer reads; the application strip's study views follow the same order and Templates stays global.",
    "QC-PIPE-003": "The Pipeline pane opens with the whole pipeline as one flow — the three input collections, the functions in order, the three output collections — followed by one section per function addressable by id; a function's permalink (`pipeline/<id>.html`) is absorbed into the Pipeline view with that function in focus.",
    "QC-FLOW-001": "One renderer draws every flow: three lanes labelled Inputs, Function and Outputs, every box a link when it has one and a plain label otherwise, a sub-line for the hash, count or file, an empty lane saying so rather than vanishing, connectors drawn by the stylesheet and nothing external.",
    "QC-FLOW-002": "A display's flow lists its datasets with the rows and hash its ARD recorded, its specification files and the study model with the analysis set it resolved, the four functions `regenerate()` calls, and the files of its current iteration — ard.json with its version, rows and hash, table.html, every RTF artifact with its hash, manifest.json; a display with no iteration draws none.",
    "QC-FLOW-003": "A value's flow reads one ARD row (or its input values) through `regenerate_values()` into its store entry; a text block's flow reads the displays and values it binds and its own source file through the block renderer and the assembler into its rendered prose in each document that places it; a document's flow reads its template, study model, displays, blocks and values through the assembler's seven gates into its JSON and HTML.",
    "QC-FLOW-004": "A dataset's flow reads the upstream file at the pinned commit (or the dataset it is derived from, or the alternate lane) through vendoring and verification and the preparation layer with its derivation count into the prepared frame every display recorded and the displays computed from it; a pipeline function's flow reads what it reads into what it writes."
  },
  "rows": [
    {
      "id": "QC-MATRIX-001",
      "area": "Extraction",
      "text": "A requirement matrix row contributes to the extract only when its ID cell is exactly a requirement ID matching `^[A-Z]{2,4}-[A-Z]+-\\d+[A-D]?$` and its requirement text is non-empty.",
      "source": "D11",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-MATRIX-002",
      "area": "Extraction",
      "text": "When a table header row is present, columns are located by name (ID, Area, Requirement, Source, Evidence Type, Status); the safety.viz positional layout is the fallback.",
      "source": "open.csr",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-MATRIX-003",
      "area": "Extraction",
      "text": "Requirement text containing an escaped pipe (`|`) is extracted intact rather than split into two cells.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-MATRIX-004",
      "area": "Extraction",
      "text": "Tables in the document that are not requirement tables, and prose containing requirement IDs, contribute nothing to the extract.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-MATRIX-005",
      "area": "Extraction",
      "text": "The committed extract is timestamp-free and a pure function of the matrix content, so the freshness guard is a plain content comparison.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DRIFT-001",
      "area": "Guards",
      "text": "`requirements --check` reports added, removed and text-changed requirement IDs and exits non-zero when the committed extract differs from a fresh extraction.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DRIFT-002",
      "area": "Guards",
      "text": "`evidence --check` reports missing tests, new tests and pass/fail changes, and ignores provenance and traceability fields entirely.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-NAME-001",
      "area": "Convention",
      "text": "Every test title in both suites matches `\"<REQ-ID>[, <REQ-ID>]: <description> (#<issue>)\"`; a guard test in each suite enforces it.",
      "source": "D11",
      "evidence": "js-unit",
      "link": "site-convention.test.js",
      "status": "draft"
    },
    {
      "id": "QC-NAME-002",
      "area": "Convention",
      "text": "Requirement IDs and issue references are parsed out of a test title, supporting several IDs and several issue refs in one title.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-EVID-001",
      "area": "Evidence",
      "text": "Vitest JSON reporter output normalizes into contracts §8 records carrying `requirementIds`, `title`, `suite`, `passed` and `file`.",
      "source": "contracts §8",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-EVID-002",
      "area": "Evidence",
      "text": "testthat results from `qc/testthat-results.json` normalize into the same record shape with `suite: \"r-unit\"`.",
      "source": "contracts §8",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-EVID-003",
      "area": "Evidence",
      "text": "Any testthat status that is not exactly `pass` (fail, error, skip) records as not passed, so the evidence page never overstates a run.",
      "source": "open.csr",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-EVID-004",
      "area": "Evidence",
      "text": "Human review of a text block records as a `text-review` suite record carrying `reviewedBy` and `reviewedAt`, so one page shows automated and human evidence together.",
      "source": "research §16",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-ROUTE-001",
      "area": "Routing",
      "text": "A JS test file routes to its module by path — `tests/unit/<module>/**` or `tests/unit/<module>-<topic>.test.js`.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-ROUTE-002",
      "area": "Routing",
      "text": "An R test file `test-<module>[-<topic>].R` routes to `<module>` by longest-prefix match against the registry.",
      "source": "research §13",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-ROUTE-003",
      "area": "Routing",
      "text": "A test that routes to no module is shared scaffold and is duplicated into every module's evidence set, so each set is self-contained.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-ROUTE-004",
      "area": "Routing",
      "text": "The module universe comes from `site/config.json` — components plus displays — so registering a display is all it takes for its evidence set to appear.",
      "source": "contracts §9",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-ROUTE-005",
      "area": "Routing",
      "text": "A component whose test files are named by topic rather than by module declares its file stems in the registry (`testPrefixes`), and the longest declared prefix wins.",
      "source": "open.csr",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-ROUTE-006",
      "area": "Routing",
      "text": "A whole suite may be owned by one component through the registry (`suites`), which is how `text-review` records route to the Text Library regardless of file path.",
      "source": "research §16",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-ROUTE-007",
      "area": "Routing",
      "text": "When a test file covers several modules, records route by the requirement-ID prefixes their owning registry entries declare; a record whose IDs no entry claims stays shared.",
      "source": "open.csr",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-EVID-005",
      "area": "Evidence",
      "text": "A text block awaiting human approval is reported as pending review and blocks assembly, but does not fail the evidence run — draft is a lifecycle state, not a defect.",
      "source": "D8",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-EVID-006",
      "area": "Evidence",
      "text": "Test-file paths in a committed evidence set are recorded relative to the repository root, including when the run happens in a git worktree, so the artifact names the repository rather than the machine that produced it.",
      "source": "contracts §8",
      "evidence": "js-unit",
      "link": "evidence-normalize.test.js",
      "status": "draft"
    },
    {
      "id": "QC-UNRES-001",
      "area": "Resolution",
      "text": "Requirement IDs referenced by a test but defined in no matrix are reported as UNRESOLVED at build time and rendered on the Quality page.",
      "source": "research §16",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-UNRES-002",
      "area": "Resolution",
      "text": "Reviewed requirements that no test references are reported as uncovered, with a coverage proportion per component.",
      "source": "research §16",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-UNRES-003",
      "area": "Resolution",
      "text": "An ID defined in a sibling component's matrix counts as resolved, not unresolved, so shared IDs do not produce false alarms.",
      "source": "research §16",
      "evidence": "js-unit",
      "link": "requirements-matrix.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-001",
      "area": "Site",
      "text": "The shared shell substitutes title, description, navigation and root prefix, so one build serves the site at any mount depth via relative URLs.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "site-render.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-002",
      "area": "Site",
      "text": "The build fails when any internal `href`/`src` in the emitted HTML does not resolve to an emitted file.",
      "source": "safety.viz",
      "evidence": "js-unit",
      "link": "site-validate.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-003",
      "area": "Site",
      "text": "The build fails when the emitted site references any external resource — script, stylesheet, font, image or CSS import — so the whole site works offline.",
      "source": "D11",
      "evidence": "js-unit",
      "link": "site-validate.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-004",
      "area": "Site",
      "text": "HTML embedded from the pipeline (a rendered display, an assembled fragment) is stripped of scripts, event handlers, external stylesheets and document wrappers before it is placed on a page.",
      "source": "open.csr",
      "evidence": "js-unit",
      "link": "site-validate.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-005",
      "area": "Site",
      "text": "A registry entry's status is derived from the filesystem — `planned` until an ARD exists, `built` once it does, `evidenced` once an evidence set does.",
      "source": "contracts §9",
      "evidence": "js-unit",
      "link": "site-render.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-006",
      "area": "Site",
      "text": "Every page renders a documented \"not yet generated\" state when its input is absent, and the build never throws on a missing or malformed input.",
      "source": "open.csr",
      "evidence": "js-unit",
      "link": "site-render.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-007",
      "area": "Site",
      "text": "A display directory present on disk but absent from the registry renders as an unregistered card rather than disappearing from the gallery.",
      "source": "open.csr",
      "evidence": "js-unit",
      "link": "site-render.test.js",
      "status": "draft"
    },
    {
      "id": "QC-CI-001",
      "area": "CI",
      "text": "CI runs the JS suite, both freshness guards and the site build on every pull request, and runs the R suite in a separate job.",
      "source": "D11",
      "evidence": "ci",
      "link": ".github/workflows/ci.yml",
      "status": "draft"
    },
    {
      "id": "QC-CI-002",
      "area": "CI",
      "text": "The built site publishes to GitHub Pages from the default branch through `actions/deploy-pages` under the `pages` concurrency group.",
      "source": "D11",
      "evidence": "ci",
      "link": ".github/workflows/pages.yml",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-001",
      "area": "Demo",
      "text": "The Demo app's selection — active tab, display, text block, focus target — round-trips losslessly through the URL hash, so any view of the report is a shareable link.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-002",
      "area": "Demo",
      "text": "An unknown or absent tab in a deep link decodes to the default tab; the four tabs are Reader, Tables, Text and Templates.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-003",
      "area": "Demo",
      "text": "A link from one pane to another pane's standalone surface resolves to a tab switch carrying the selection it named — a display permalink selects that display, a text-block anchor selects that block — at any authored path depth.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-004",
      "area": "Demo",
      "text": "Links that are not pane crossings are left to navigate: Quality, Design & Research, absolute URLs, and bare fragments (which are in-pane scrolling).",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-005",
      "area": "Demo",
      "text": "A crossing that names no display keeps the display already selected, so moving between panes does not silently reset what the visitor was looking at.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-006",
      "area": "Demo",
      "text": "A display slug that is not in the build falls back to the first available display rather than rendering a blank pane.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-007",
      "area": "Demo",
      "text": "Every pane is server-rendered and present in the emitted HTML with exactly one visible; the app's tablist uses its own class vocabulary and carries the tablist/tab/tabpanel ARIA wiring.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-008",
      "area": "Demo",
      "text": "A tab whose pane did not render is dropped from the tablist rather than shown empty.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-009",
      "area": "Demo",
      "text": "The Tables pane renders one panel per display and one picker entry per display, shows exactly one, opens on the requested display, and falls back to the first when the request is unknown.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-010",
      "area": "Demo",
      "text": "An empty display library renders the documented empty state rather than an app with no content.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-011",
      "area": "Demo",
      "text": "The Templates pane renders every section of the E3 model, marking those this report does not populate, rather than trimming the skeleton to what happens to be built.",
      "source": "D6",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-012",
      "area": "Demo",
      "text": "Display numbers in the Templates pane are derived from assembly order by the same tested function the assembler uses, never authored; reordering the assembly moves every number and changes no slug.",
      "source": "D6",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-013",
      "area": "Demo",
      "text": "The Templates pane renders its documented empty state when the section model is missing, and renders correctly on a tree where the assembler has never run.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-014",
      "area": "Demo",
      "text": "The auto-generated provenance section is identified as generated in the Templates pane.",
      "source": "D6",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-016",
      "area": "Demo",
      "text": "The application strip's four views are real links to real pages, marked with `aria-current` rather than tab-selection state, so the bar is working navigation with no JavaScript and the client only upgrades it.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-017",
      "area": "Demo",
      "text": "The application strip carries a live context readout — study, assigned display number, slug, iteration and short ARD hash — built from an embedded index that is escaped so it cannot close its own script element.",
      "source": "demo-layout §2",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-018",
      "area": "Demo",
      "text": "The navigation tree is rooted at the study, with documents, displays and text as the collections beneath it; more than one document is listed, and one that is not built is marked rather than hidden.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-019",
      "area": "Demo",
      "text": "A display records every document in the library that places it — not only the primary one — and the explorer states no display number at all; a text block that is not on disk is left out of the tree while a draft block is flagged in it.",
      "source": "open.csr#42",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-020",
      "area": "Demo",
      "text": "The explorer renders the study, every group with its item count, opens only the active group, and marks the selected item.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-021",
      "area": "Demo",
      "text": "Every selectable item in the explorer is a real link carrying its own deep link, and an unbuilt item is not a link at all.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-022",
      "area": "Demo",
      "text": "The header carries a Read/Edit mode control in which Read is current and Edit is genuinely disabled, and the explorer renders without throwing on an empty registry.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-023",
      "area": "Demo",
      "text": "A built document contributes its own top-level sections to the tree, and a section counts as populated when it or anything beneath it is; an unbuilt document contributes none.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-024",
      "area": "Demo",
      "text": "Sections render beneath their own document, each a deep link to its heading, with unpopulated sections still navigable and labelled as modelled-but-not-populated.",
      "source": "#113",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-025",
      "area": "Demo",
      "text": "Expansion and selection are one state, not two: the open document shows its table of contents and every other document shows its title alone, with no per-document disclosure control in the markup, no per-document collapse flag in the client, and nothing in the stylesheet that can hide the open document's contents. Group-level collapse is unaffected.",
      "source": "open.csr#40",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-026",
      "area": "Demo",
      "text": "Sections the report does not populate are dimmed as a whole row and keep the tooltip that explains why, while populated sections stay at full contrast; a group with no items renders as a heading rather than an empty folder.",
      "source": "open.csr#10",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-027",
      "area": "Demo",
      "text": "The Values pane shows every value's name, number and provenance — an ARD address with its iteration and hash, or the declared arithmetic for a derived value — and the text blocks that cite it.",
      "source": "#129",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-028",
      "area": "Demo",
      "text": "The Values pane states the verdict of the gate that re-derived the store against the committed ARDs, and renders a documented empty state when no store exists.",
      "source": "#129",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-029",
      "area": "Demo",
      "text": "Values are a collection of the study in the navigation tree, listed with their formatted numbers, and selecting one focuses it in the pane rather than introducing a fourth selection key.",
      "source": "#129",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-030",
      "area": "Demo",
      "text": "A link to any document's standalone reader page resolves to the Documents view naming that document — the primary document's page by the key `index`, every other template object by its own — so a second document is absorbed into the app the way a display permalink already is.",
      "source": "open.csr#36",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-031",
      "area": "Demo",
      "text": "A document selection resolves against the documents the page actually rendered, accepting either a document id or the basename of that document's reader page, and falls back to the first rendered document rather than to an empty pane.",
      "source": "open.csr#36",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-032",
      "area": "Demo",
      "text": "The Documents pane renders one panel per assembled document from the same reader renderer, keyed by both document id and reader-page basename, shows exactly one, and hoists a single trace panel and index above the set rather than emitting one per document.",
      "source": "open.csr#36",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-033",
      "area": "Demo",
      "text": "A document the page rendered is a selectable explorer node carrying its standalone permalink as the no-JavaScript fallback; a document the page did not render stays an ordinary link out with no selection attributes.",
      "source": "open.csr#36",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-034",
      "area": "Demo",
      "text": "Every rendered document contributes its own top-level sections to the explorer, each section link naming its document, and a section slug is resolved inside the document on screen rather than across the page.",
      "source": "open.csr#36",
      "evidence": "js-unit",
      "link": "site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DEMO-035",
      "area": "Demo",
      "text": "Each document panel carries its own prose disclosure, so a document whose blocks are unreviewed reads as draft inside the app exactly as it does on its own page.",
      "source": "open.csr#36",
      "evidence": "js-unit",
      "link": "site-demo.test.js, site-documents.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-013",
      "area": "Site",
      "text": "The display store states which documents place each display, built over the whole template library: the display's own page lists every document with a link to it and the number that document assigned, a display no document places says so, and the gallery card names those documents rather than counting them.",
      "source": "open.csr#42",
      "evidence": "js-unit",
      "link": "site-render.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-014",
      "area": "Site",
      "text": "A display placed in a document carries a hyperlink to its entry in the display store, written in the form the app's link-interception rule recognises, so the same href is a pane switch inside the demo and a real navigation on the standalone reader page.",
      "source": "open.csr#42",
      "evidence": "js-unit",
      "link": "site-render.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-015",
      "area": "Site",
      "text": "A display's page lists the requirements that cover that display — the matrix rows matching the prefixes it declares in `site/config.json`, together with the ids its own evidence cites — and not the whole matrix. A display declaring no prefixes lists none rather than all of them.",
      "source": "Correctness",
      "evidence": "`display-requirements.test.js`",
      "link": "Verified",
      "status": ""
    },
    {
      "id": "QC-SITE-012",
      "area": "Site",
      "text": "The stylesheet expresses the explorer's one state and only that: a document's section list is hidden by default and revealed by sitting next to the current document, with no per-node collapse class and no disclosure-control rules left to override it.",
      "source": "open.csr#40",
      "evidence": "js-unit",
      "link": "site-css.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-011",
      "area": "Site",
      "text": "Two documents assembled from the shared libraries quote the same artifacts: every display both documents place resolves to the same ARD source and the same ARD payload, and both carry the same value store with no value differing in raw value or formatted string. Only the per-document display number differs.",
      "source": "open.csr#36",
      "evidence": "js-unit",
      "link": "site-documents.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-008",
      "area": "Site",
      "text": "`site/site.css` parses as a whole: every block is closed, no block is closed that was never opened, and no declaration sits outside a rule — so no rule in the file can be silently dead in the browser.",
      "source": "open.csr#16",
      "evidence": "js-unit",
      "link": "site-css.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-009",
      "area": "Site",
      "text": "The site publishes one reader page and one template page for every assembled template object in `library/templates/`, discovered from the library rather than declared: an object absent from `site/config.json` still publishes, titled from its own model, and every document reaches every other from the switcher, the navigation tree and the home page.",
      "source": "open.csr#32",
      "evidence": "js-unit",
      "link": "site-documents.test.js",
      "status": "draft"
    },
    {
      "id": "QC-SITE-010",
      "area": "Site",
      "text": "Every document states whether its own prose has been reviewed, derived from the `approval.state` the assembler recorded on each block it placed: a document holding unapproved blocks carries a notice above its first section and a flag in the switcher, the navigation tree and its home-page card; a document whose blocks are all approved says that instead.",
      "source": "open.csr#32",
      "evidence": "js-unit",
      "link": "site-documents.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DATA-001",
      "area": "Data",
      "text": "The site reads a data registry (`site/config.json` → `data`) together with the vendored package's `PROVENANCE.json`: every registered dataset carries its upstream path, blob SHA-1, SHA-256 and byte counts and the package its pinned commit and licence; a registered dataset with no vendored file is planned, a derived or alternate-lane dataset is not reported as a missing file, and a vendored file nobody registered is published with a warning, the way an unregistered display is. A repository with no registry renders the empty state.",
      "source": "open.csr#76",
      "evidence": "js-unit",
      "link": "site-data.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DATA-002",
      "area": "Data",
      "text": "The reverse index is built from the ARDs alone: a display whose current ARD names a dataset in its provenance envelope appears under that dataset with the iteration, row count, prepared hash and ARD hash the envelope recorded; a dataset no ARD names has an empty entry; a dataset an ARD names that the registry does not know still gets one.",
      "source": "open.csr#76",
      "evidence": "js-unit",
      "link": "site-data.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DATA-003",
      "area": "Data",
      "text": "The Data pane is one page with the package record, one section per dataset (facts, derivation notes, the read-by table linking each display in the form the app absorbs, the upstream path linking the source repository at the pinned commit, a derived dataset linking what it was derived from) and the lanes section naming both packagings and listing only the variables the agreement record says differ; each dataset also has a standalone page with the same facts.",
      "source": "open.csr#76",
      "evidence": "js-unit",
      "link": "site-data.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DATA-004",
      "area": "Data",
      "text": "Given the data index, a display's header links each dataset it declares to that dataset's page, carrying as its title the row count and hash THIS iteration's ARD recorded and a short hash beside the link; a dataset the header names but the ARD does not is still linked and says so; without the index the labels print as before.",
      "source": "open.csr#76",
      "evidence": "js-unit",
      "link": "site-data.test.js",
      "status": "draft"
    },
    {
      "id": "QC-DATA-005",
      "area": "Data",
      "text": "Data is a view of the study after Values and a collection in the explorer with one item per dataset (its id as the number, planned when not vendored) plus the lanes; a dataset permalink (`data/<id>.html`) is absorbed into the Data view with that dataset in focus, the lanes and the index likewise, at any authored depth.",
      "source": "open.csr#76",
      "evidence": "js-unit",
      "link": "site-data.test.js, site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-META-001",
      "area": "Metadata",
      "text": "The study page renders `library/study.yaml` as declared: id, title, phase, cut-off and source, the arms in print order, the columns that carry an arm label with the assignment each means, and every analysis set with its label, flag and subjects per arm totalled — linking to the Data section for the source; a repository without a study model renders the empty state and reports it in the build warnings.",
      "source": "open.csr#77",
      "evidence": "js-unit",
      "link": "site-metadata.test.js",
      "status": "draft"
    },
    {
      "id": "QC-META-002",
      "area": "Metadata",
      "text": "The specifications page lists every display with its analysis and display files, its current iteration's spec, display and ARD hashes, and every iteration with the actor, change request and hashes its manifest recorded (either manifest shape); custom code shared through `custom_from` is stated from the borrowing side and the owning side; value declarations are read from `library/values/values.yaml` with their kind and declaration, each linking to the store.",
      "source": "open.csr#77",
      "evidence": "js-unit",
      "link": "site-metadata.test.js",
      "status": "draft"
    },
    {
      "id": "QC-META-003",
      "area": "Metadata",
      "text": "The approvals page lists every text block with its tier, version, approval state, approver, date and drafting model from its frontmatter, with counts of approved, draft and generated blocks held out of the report, each block linking to the Text view.",
      "source": "open.csr#77",
      "evidence": "js-unit",
      "link": "site-metadata.test.js",
      "status": "draft"
    },
    {
      "id": "QC-META-004",
      "area": "Metadata",
      "text": "Environments are every distinct R, operating-system and package-version record across every iteration of every display, each listing the iterations built in it; document models are the template objects on disk with sections, slots and the documents assembled from each, an empty library saying so; the requirements page lists every matrix under `quality/requirements/` with its row count, its source link and a route to its evidence page.",
      "source": "open.csr#77",
      "evidence": "js-unit",
      "link": "site-metadata.test.js",
      "status": "draft"
    },
    {
      "id": "QC-META-005",
      "area": "Metadata",
      "text": "Metadata is a view of the study after Data and a collection in the explorer with one item per page (the study id, the counts as numbers); the pane carries every section by id; a page permalink (`metadata/<page>.html`) is absorbed into the view with that section in focus and a fragment on it selects the deeper anchor; the Templates view keeps its own tab and permalink as the shortcut to the document models.",
      "source": "open.csr#77",
      "evidence": "js-unit",
      "link": "site-metadata.test.js, site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-LINK-001",
      "area": "Links",
      "text": "Given the links, a display's header names the analysis set as the study model defines it (label and flag) and the grouping column, the specification history (iteration count), the environment (R version), the text blocks that bind it and the values sourced from it — each a link into the view that owns it; without the links the header prints as before; an empty list says so rather than printing nothing.",
      "source": "open.csr#78",
      "evidence": "js-unit",
      "link": "site-links.test.js",
      "status": "draft"
    },
    {
      "id": "QC-LINK-002",
      "area": "Links",
      "text": "The assembler records what a document was built from (`inputs`: the template object, the study model, every placed display with its iteration, ARD hash and datasets, every block with its state and derived inputs, every value cited, every dataset reached with the displays that read it) and, per block, the displays its bindings resolved against with iteration and hash; the Reader renders that record as a panel of links above the text and the provenance appendix as links to each display, each dataset and the environment.",
      "source": "open.csr#78",
      "evidence": "js-unit",
      "link": "site-links.test.js, assemble-document.test.js",
      "status": "draft"
    },
    {
      "id": "QC-LINK-003",
      "area": "Links",
      "text": "A text block's card lists its inputs derived from its bindings — each display bound with the iteration and ARD hash resolved against, the values cited, its section and its approval, each a link — and names a declared display the block binds nothing from as drift; the frontmatter's `displays:` list stays the author's declaration, the build already failing a block that binds an undeclared display.",
      "source": "open.csr#78",
      "evidence": "js-unit",
      "link": "site-links.test.js",
      "status": "draft"
    },
    {
      "id": "QC-LINK-004",
      "area": "Links",
      "text": "The values pane links the arm a source address names to the study model, the datasets behind the value's display to the Data section, and the documents that place the blocks citing it; the arm link needs no index and is always present, the data and document links appear only when their indexes are given.",
      "source": "open.csr#78",
      "evidence": "js-unit",
      "link": "site-links.test.js",
      "status": "draft"
    },
    {
      "id": "QC-LINK-005",
      "area": "Links",
      "text": "A document whose build failed a gate is flagged in the explorer (`gates fail`), one that passed is not, and one not yet assembled carries no verdict.",
      "source": "open.csr#78",
      "evidence": "js-unit",
      "link": "site-links.test.js",
      "status": "draft"
    },
    {
      "id": "QC-PIPE-001",
      "area": "Pipeline",
      "text": "The registry (`site/config.json` → `pipeline.functions`) declares the pipeline's functions by the code's own names with kind, code path, what each reads and writes, what it calls and its gates; the site builds one page per function stating those, linking the code, and listing every element the function produced — datasets read, displays with a current iteration, values in the store, documents assembled — each a link back; a call to an unregistered function is a build warning.",
      "source": "open.csr#82",
      "evidence": "js-unit",
      "link": "site-pipeline.test.js",
      "status": "draft"
    },
    {
      "id": "QC-PIPE-002",
      "area": "Pipeline",
      "text": "The explorer arranges its groups in three parts in the pipeline's order — Inputs (Data, Metadata, Text), Pipeline (the functions), Outputs (Displays, Values, Documents) — each part headed and noted, while `groups` stays the flat list every consumer reads; the application strip's study views follow the same order and Templates stays global.",
      "source": "open.csr#82",
      "evidence": "js-unit",
      "link": "site-pipeline.test.js, site-demo.test.js",
      "status": "draft"
    },
    {
      "id": "QC-PIPE-003",
      "area": "Pipeline",
      "text": "The Pipeline pane opens with the whole pipeline as one flow — the three input collections, the functions in order, the three output collections — followed by one section per function addressable by id; a function's permalink (`pipeline/<id>.html`) is absorbed into the Pipeline view with that function in focus.",
      "source": "open.csr#82",
      "evidence": "js-unit",
      "link": "site-pipeline.test.js",
      "status": "draft"
    },
    {
      "id": "QC-FLOW-001",
      "area": "Flow",
      "text": "One renderer draws every flow: three lanes labelled Inputs, Function and Outputs, every box a link when it has one and a plain label otherwise, a sub-line for the hash, count or file, an empty lane saying so rather than vanishing, connectors drawn by the stylesheet and nothing external.",
      "source": "open.csr#83",
      "evidence": "js-unit",
      "link": "site-flow.test.js",
      "status": "draft"
    },
    {
      "id": "QC-FLOW-002",
      "area": "Flow",
      "text": "A display's flow lists its datasets with the rows and hash its ARD recorded, its specification files and the study model with the analysis set it resolved, the four functions `regenerate()` calls, and the files of its current iteration — ard.json with its version, rows and hash, table.html, every RTF artifact with its hash, manifest.json; a display with no iteration draws none.",
      "source": "open.csr#83",
      "evidence": "js-unit",
      "link": "site-flow.test.js",
      "status": "draft"
    },
    {
      "id": "QC-FLOW-003",
      "area": "Flow",
      "text": "A value's flow reads one ARD row (or its input values) through `regenerate_values()` into its store entry; a text block's flow reads the displays and values it binds and its own source file through the block renderer and the assembler into its rendered prose in each document that places it; a document's flow reads its template, study model, displays, blocks and values through the assembler's seven gates into its JSON and HTML.",
      "source": "open.csr#83",
      "evidence": "js-unit",
      "link": "site-flow.test.js",
      "status": "draft"
    },
    {
      "id": "QC-FLOW-004",
      "area": "Flow",
      "text": "A dataset's flow reads the upstream file at the pinned commit (or the dataset it is derived from, or the alternate lane) through vendoring and verification and the preparation layer with its derivation count into the prepared frame every display recorded and the displays computed from it; a pipeline function's flow reads what it reads into what it writes.",
      "source": "open.csr#83",
      "evidence": "js-unit",
      "link": "site-flow.test.js",
      "status": "draft"
    }
  ]
}
