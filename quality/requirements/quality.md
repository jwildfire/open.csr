# Quality Framework — Requirements

The requirements → tests → evidence loop itself: how a reviewed requirement becomes a test title, how two test suites in two languages become one evidence stream, and what fails the build when a committed artifact stops telling the truth.

Ported from the safety.viz framework (see [research §05](../../research/sections/05_safetyviz-evidence-framework.md)) with open.csr's additions: a testthat normalizer beside the vitest one, a traceability object on every evidence set, human review modelled as its own suite, and a build-time report of unresolved requirement IDs — the blind spot safety.viz degrades through silently.

## Requirement context

Design decision **D11** (see [design §3](../../docs/design/design.md)) fixes the framework in place: matrices in `quality/requirements/`, the ID regex `^[A-Z]{2,4}-[A-Z]+-\d+[A-D]?$` unchanged, test titles carrying IDs, testthat + vitest normalizing into one `evidence.json` per module, published as evidence pages. [Contracts §8](../../docs/design/contracts.md) is normative for the record shape.

## Scope

In scope: matrix extraction, evidence normalization and routing, the freshness guards, the naming convention and its guard tests, the unresolved-ID report, and the static site that publishes all of it.

Out of scope: the statistical content of any display (see `displays.md`), the prose gates on text blocks (see `text.md`), and document assembly (see `templates.md`).

## Source inventory

- `scripts/requirements-lib.mjs`, `scripts/requirements.mjs` — matrix → `docs/requirements/<component>.json`
- `scripts/evidence-lib.mjs`, `scripts/evidence.mjs` — suites → `docs/evidence/<module>/evidence.json`
- `scripts/site-lib.mjs`, `scripts/site.mjs`, `site/config.json`, `site/shell.html`, `site/site.css`
- `.github/workflows/ci.yml`, `.github/workflows/pages.yml`

## Requirements

| ID | Area | Requirement | Source | Evidence Type | Test/Evidence Link | Status | AI Review | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| QC-MATRIX-001 | Extraction | A requirement matrix row contributes to the extract only when its ID cell is exactly a requirement ID matching `^[A-Z]{2,4}-[A-Z]+-\d+[A-D]?$` and its requirement text is non-empty. | D11 | js-unit | requirements-matrix.test.js | draft | ai-drafted | Structural, so header and separator rows are skipped without hard-coding table position. |
| QC-MATRIX-002 | Extraction | When a table header row is present, columns are located by name (ID, Area, Requirement, Source, Evidence Type, Status); the safety.viz positional layout is the fallback. | open.csr | js-unit | requirements-matrix.test.js | draft | ai-drafted | Matrices are authored by several agents, so column order cannot be assumed. |
| QC-MATRIX-003 | Extraction | Requirement text containing an escaped pipe (`\|`) is extracted intact rather than split into two cells. | safety.viz | js-unit | requirements-matrix.test.js | draft | ai-drafted | |
| QC-MATRIX-004 | Extraction | Tables in the document that are not requirement tables, and prose containing requirement IDs, contribute nothing to the extract. | safety.viz | js-unit | requirements-matrix.test.js | draft | ai-drafted | |
| QC-MATRIX-005 | Extraction | The committed extract is timestamp-free and a pure function of the matrix content, so the freshness guard is a plain content comparison. | safety.viz | js-unit | requirements-matrix.test.js | draft | ai-drafted | Volatile provenance is quarantined in the evidence set, never the extract. |
| QC-DRIFT-001 | Guards | `requirements --check` reports added, removed and text-changed requirement IDs and exits non-zero when the committed extract differs from a fresh extraction. | safety.viz | js-unit | requirements-matrix.test.js | draft | ai-drafted | |
| QC-DRIFT-002 | Guards | `evidence --check` reports missing tests, new tests and pass/fail changes, and ignores provenance and traceability fields entirely. | safety.viz | js-unit | evidence-normalize.test.js | draft | ai-drafted | Provenance in the guard would make it noise, and noisy guards get switched off. |
| QC-NAME-001 | Convention | Every test title in both suites matches `"<REQ-ID>[, <REQ-ID>]: <description> (#<issue>)"`; a guard test in each suite enforces it. | D11 | js-unit | site-convention.test.js | draft | ai-drafted | Retrofitting IDs onto an existing suite is the expensive path — the guard ships on day one. |
| QC-NAME-002 | Convention | Requirement IDs and issue references are parsed out of a test title, supporting several IDs and several issue refs in one title. | safety.viz | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-EVID-001 | Evidence | Vitest JSON reporter output normalizes into contracts §8 records carrying `requirementIds`, `title`, `suite`, `passed` and `file`. | contracts §8 | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-EVID-002 | Evidence | testthat results from `qc/testthat-results.json` normalize into the same record shape with `suite: "r-unit"`. | contracts §8 | js-unit | evidence-normalize.test.js | draft | ai-drafted | R owns the statistics; both suites feed one evidence stream. |
| QC-EVID-003 | Evidence | Any testthat status that is not exactly `pass` (fail, error, skip) records as not passed, so the evidence page never overstates a run. | open.csr | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-EVID-004 | Evidence | Human review of a text block records as a `text-review` suite record carrying `reviewedBy` and `reviewedAt`, so one page shows automated and human evidence together. | research §16 | js-unit | evidence-normalize.test.js | draft | ai-drafted | A suite cannot produce medical-writer sign-off. |
| QC-ROUTE-001 | Routing | A JS test file routes to its module by path — `tests/unit/<module>/**` or `tests/unit/<module>-<topic>.test.js`. | safety.viz | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-ROUTE-002 | Routing | An R test file `test-<module>[-<topic>].R` routes to `<module>` by longest-prefix match against the registry. | research §13 | js-unit | evidence-normalize.test.js | draft | ai-drafted | `test-t-ae-overview-ard.R` must beat a hypothetical `t` module. |
| QC-ROUTE-003 | Routing | A test that routes to no module is shared scaffold and is duplicated into every module's evidence set, so each set is self-contained. | safety.viz | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-ROUTE-004 | Routing | The module universe comes from `site/config.json` — components plus displays — so registering a display is all it takes for its evidence set to appear. | contracts §9 | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-ROUTE-005 | Routing | A component whose test files are named by topic rather than by module declares its file stems in the registry (`testPrefixes`), and the longest declared prefix wins. | open.csr | js-unit | evidence-normalize.test.js | draft | ai-drafted | `test-ard-build.R` belongs to the engine; `site-binding.test.js` belongs to traceability. |
| QC-ROUTE-006 | Routing | A whole suite may be owned by one component through the registry (`suites`), which is how `text-review` records route to the Text Library regardless of file path. | research §16 | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-ROUTE-007 | Routing | When a test file covers several modules, records route by the requirement-ID prefixes their owning registry entries declare; a record whose IDs no entry claims stays shared. | open.csr | js-unit | evidence-normalize.test.js | draft | ai-drafted | `test-displays.R` holds every display's assertions; `DSP-ALL-*` is deliberately cross-cutting. |
| QC-EVID-005 | Evidence | A text block awaiting human approval is reported as pending review and blocks assembly, but does not fail the evidence run — draft is a lifecycle state, not a defect. | D8 | js-unit | evidence-normalize.test.js | draft | ai-drafted | |
| QC-EVID-006 | Evidence | Test-file paths in a committed evidence set are recorded relative to the repository root, including when the run happens in a git worktree, so the artifact names the repository rather than the machine that produced it. | contracts §8 | js-unit | evidence-normalize.test.js | draft | ai-drafted | Vitest and testthat both report absolute paths. A path outside the repository is left visible rather than mangled — that would be a bug worth seeing, not one worth hiding. |
| QC-UNRES-001 | Resolution | Requirement IDs referenced by a test but defined in no matrix are reported as UNRESOLVED at build time and rendered on the Quality page. | research §16 | js-unit | requirements-matrix.test.js | draft | ai-drafted | The safety.viz blind spot: exact-match resolution degrades silently today. |
| QC-UNRES-002 | Resolution | Reviewed requirements that no test references are reported as uncovered, with a coverage proportion per component. | research §16 | js-unit | requirements-matrix.test.js | draft | ai-drafted | |
| QC-UNRES-003 | Resolution | An ID defined in a sibling component's matrix counts as resolved, not unresolved, so shared IDs do not produce false alarms. | research §16 | js-unit | requirements-matrix.test.js | draft | ai-drafted | |
| QC-SITE-001 | Site | The shared shell substitutes title, description, navigation and root prefix, so one build serves the site at any mount depth via relative URLs. | safety.viz | js-unit | site-render.test.js | draft | ai-drafted | |
| QC-SITE-002 | Site | The build fails when any internal `href`/`src` in the emitted HTML does not resolve to an emitted file. | safety.viz | js-unit | site-validate.test.js | draft | ai-drafted | A broken site can never publish. |
| QC-SITE-003 | Site | The build fails when the emitted site references any external resource — script, stylesheet, font, image or CSS import — so the whole site works offline. | D11 | js-unit | site-validate.test.js | draft | ai-drafted | Outbound `<a href>` links are navigation, not a runtime dependency. |
| QC-SITE-004 | Site | HTML embedded from the pipeline (a rendered display, an assembled fragment) is stripped of scripts, event handlers, external stylesheets and document wrappers before it is placed on a page. | open.csr | js-unit | site-validate.test.js | draft | ai-drafted | |
| QC-SITE-005 | Site | A registry entry's status is derived from the filesystem — `planned` until an ARD exists, `built` once it does, `evidenced` once an evidence set does. | contracts §9 | js-unit | site-render.test.js | draft | ai-drafted | The registry declares intent; the filesystem decides. |
| QC-SITE-006 | Site | Every page renders a documented "not yet generated" state when its input is absent, and the build never throws on a missing or malformed input. | open.csr | js-unit | site-render.test.js | draft | ai-drafted | The pipeline, the assembler and the site are built in parallel. |
| QC-SITE-007 | Site | A display directory present on disk but absent from the registry renders as an unregistered card rather than disappearing from the gallery. | open.csr | js-unit | site-render.test.js | draft | ai-drafted | Slug drift surfaces instead of hiding. |
| QC-CI-001 | CI | CI runs the JS suite, both freshness guards and the site build on every pull request, and runs the R suite in a separate job. | D11 | ci | .github/workflows/ci.yml | draft | ai-drafted | Evidence for this row is the workflow run itself. |
| QC-CI-002 | CI | The built site publishes to GitHub Pages from the default branch through `actions/deploy-pages` under the `pages` concurrency group. | D11 | ci | .github/workflows/pages.yml | draft | ai-drafted | |

## The Demo surface (repo #1, requirement #113)

The four browsing surfaces as panes of one view, sharing a selection. Source: `scripts/app-lib.mjs`, `site/demo/core.js`, `site/demo/client.js`, `scripts/text-status-lib.mjs`.

| ID | Area | Requirement | Source | Evidence Type | Test/Evidence Link | Status | AI Review | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| QC-DEMO-001 | Demo | The Demo app's selection — active tab, display, text block, focus target — round-trips losslessly through the URL hash, so any view of the report is a shareable link. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Empty parts are omitted, keeping the common case short. |
| QC-DEMO-002 | Demo | An unknown or absent tab in a deep link decodes to the default tab; the four tabs are Reader, Tables, Text and Templates. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | A stale or hand-edited link degrades to a working app rather than an empty one. |
| QC-DEMO-003 | Demo | A link from one pane to another pane's standalone surface resolves to a tab switch carrying the selection it named — a display permalink selects that display, a text-block anchor selects that block — at any authored path depth. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | This is the whole absorption mechanism: the panes are the unmodified standalone surfaces, and cross-pane behaviour is one link-interception rule rather than a change to every renderer. |
| QC-DEMO-004 | Demo | Links that are not pane crossings are left to navigate: Quality, Design & Research, absolute URLs, and bare fragments (which are in-pane scrolling). | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Intercepting a bare fragment would break scrolling inside the visible pane. |
| QC-DEMO-005 | Demo | A crossing that names no display keeps the display already selected, so moving between panes does not silently reset what the visitor was looking at. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Persistence across panes is what makes the selection *shared* rather than per-pane. |
| QC-DEMO-006 | Demo | A display slug that is not in the build falls back to the first available display rather than rendering a blank pane. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | |
| QC-DEMO-007 | Demo | Every pane is server-rendered and present in the emitted HTML with exactly one visible; the app's tablist uses its own class vocabulary and carries the tablist/tab/tabpanel ARIA wiring. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Panes must not be lazily injected: the app client binds its listeners once at module load, so an injected pane would get none. The shell's own tab controller pairs `.tab` with `.tab-panel` by index across a whole group, so the outer app reusing those names would desync the inner per-display tabs. |
| QC-DEMO-008 | Demo | A tab whose pane did not render is dropped from the tablist rather than shown empty. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | |
| QC-DEMO-009 | Demo | The Tables pane renders one panel per display and one picker entry per display, shows exactly one, opens on the requested display, and falls back to the first when the request is unknown. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | |
| QC-DEMO-010 | Demo | An empty display library renders the documented empty state rather than an app with no content. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Same day-one contract as QC-SITE-006. |
| QC-DEMO-011 | Demo | The Templates pane renders every section of the E3 model, marking those this report does not populate, rather than trimming the skeleton to what happens to be built. | D6 | js-unit | site-demo.test.js | draft | ai-drafted | The full skeleton staying visible is the honest statement of demo scope. |
| QC-DEMO-012 | Demo | Display numbers in the Templates pane are derived from assembly order by the same tested function the assembler uses, never authored; reordering the assembly moves every number and changes no slug. | D6 | js-unit | site-demo.test.js | draft | ai-drafted | The pane cannot disagree with the document because it does not carry a second numbering implementation. |
| QC-DEMO-013 | Demo | The Templates pane renders its documented empty state when the section model is missing, and renders correctly on a tree where the assembler has never run. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | It reads the template model rather than the assembled document. |
| QC-DEMO-014 | Demo | The auto-generated provenance section is identified as generated in the Templates pane. | D6 | js-unit | site-demo.test.js | draft | ai-drafted | E3 reserved the slot in 1995; open.csr fills it mechanically. |
| QC-DEMO-016 | Demo | The application strip's four views are real links to real pages, marked with `aria-current` rather than tab-selection state, so the bar is working navigation with no JavaScript and the client only upgrades it. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Navigation semantics, not tab semantics: these links change the URL and work unenhanced, so announcing them as tabs would misdescribe them. Requires a permalink per view, which is why `/templates/` is emitted. |
| QC-DEMO-017 | Demo | The application strip carries a live context readout — study, assigned display number, slug, iteration and short ARD hash — built from an embedded index that is escaped so it cannot close its own script element. | demo-layout §2 | js-unit | site-demo.test.js | draft | ai-drafted | Identity and provenance live in the chrome, not behind a properties dialog: in a regulated document "which artifact, which version, which hash" is the first question, not a detail. A display with no iteration yet contributes nulls rather than a broken readout. |
| QC-DEMO-018 | Demo | The navigation tree is rooted at the study, with documents, displays and text as the collections beneath it; more than one document is listed, and one that is not built is marked rather than hidden. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | The registry is data (`site/config.json` `documents`), so adding a second document — a SAP, an ISS — is a config edit rather than a code change. |
| QC-DEMO-019 | Demo | A display records every document in the library that places it — not only the primary one — and the explorer states no display number at all; a text block that is not on disk is left out of the tree while a draft block is flagged in it. | open.csr#42 | js-unit | site-demo.test.js | draft | ai-drafted | The same display can appear in more than one document, so displays are a peer collection of documents rather than a child of one — and `usedIn` was built from the primary document's index alone, so it could only ever hold one name however many documents placed the display. The number is the same mistake in the other direction: a display is identified by its slug and the number belongs to the assembly, so the AE overview is Table 14.3.1.2 in the report and Table 13.4 in the synopsis. A number on a surface that serves every document at once is one report's fact printed where four assemblies disagree. |
| QC-DEMO-020 | Demo | The explorer renders the study, every group with its item count, opens only the active group, and marks the selected item. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Groups the visitor opened stay open across view changes; collapsing them on every switch would fight the person using it. |
| QC-DEMO-021 | Demo | Every selectable item in the explorer is a real link carrying its own deep link, and an unbuilt item is not a link at all. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Same progressive-enhancement contract as the header views (QC-DEMO-016). |
| QC-DEMO-022 | Demo | The header carries a Read/Edit mode control in which Read is current and Edit is genuinely disabled, and the explorer renders without throwing on an empty registry. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Editing arrives with the spec editor (#113 increment B). A control that looks live but does nothing is worse than one that says so. |
| QC-DEMO-023 | Demo | A built document contributes its own top-level sections to the tree, and a section counts as populated when it or anything beneath it is; an unbuilt document contributes none. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | ICH E3 puts content in subsections — 12.2.1 carries the AE summary, not section 12 — so testing only the top-level flag would report almost the whole report empty. |
| QC-DEMO-024 | Demo | Sections render beneath their own document, each a deep link to its heading, with unpopulated sections still navigable and labelled as modelled-but-not-populated. | #113 | js-unit | site-demo.test.js | draft | ai-drafted | Replaces the reader's separate table of contents: which document and which section are the same question asked twice, and one place to ask it is enough. |
| QC-DEMO-025 | Demo | Expansion and selection are one state, not two: the open document shows its table of contents and every other document shows its title alone, with no per-document disclosure control in the markup, no per-document collapse flag in the client, and nothing in the stylesheet that can hide the open document's contents. Group-level collapse is unaffected. | open.csr#40 | js-unit | site-demo.test.js | draft | ai-drafted | The tree used to answer "are this document's contents showing?" twice — once by which document is open, once by a remembered per-node flag — and the two answers were independent. The arrow on a document that was not open moved and revealed nothing; the arrow on the one that was open hid the contents the tree exists to list. Two states where one will do is the bug, so the fix is a subtraction rather than a cleverer arrow. Which collection you are looking at is a genuinely separate question and keeps its own caret. |
| QC-DEMO-026 | Demo | Sections the report does not populate are dimmed as a whole row and keep the tooltip that explains why, while populated sections stay at full contrast; a group with no items renders as a heading rather than an empty folder. | open.csr#10 | js-unit | site-demo.test.js | draft | ai-drafted | Sixteen modelled sections of which a few are filled should read as exactly that at a glance. Dimmed, not hidden — the heading really is in the assembled document, saying it is not part of this demonstration. |
| QC-DEMO-027 | Demo | The Values pane shows every value's name, number and provenance — an ARD address with its iteration and hash, or the declared arithmetic for a derived value — and the text blocks that cite it. | #129 | js-unit | site-demo.test.js | draft | ai-drafted | A derived value shows its arithmetic rather than an address it does not have; pretending otherwise would be the lie the store exists to prevent. |
| QC-DEMO-028 | Demo | The Values pane states the verdict of the gate that re-derived the store against the committed ARDs, and renders a documented empty state when no store exists. | #129 | js-unit | site-demo.test.js | draft | ai-drafted | The pane asserts freshness rather than implying it: the store is a committed artifact, so "these matched at the last assembly" is the load-bearing claim. |
| QC-DEMO-029 | Demo | Values are a collection of the study in the navigation tree, listed with their formatted numbers, and selecting one focuses it in the pane rather than introducing a fourth selection key. | #129 | js-unit | site-demo.test.js | draft | ai-drafted | The pane is one list, and `focus` already means "the thing in this pane I am looking at". |
| QC-DEMO-030 | Demo | A link to any document's standalone reader page resolves to the Documents view naming that document — the primary document's page by the key `index`, every other template object by its own — so a second document is absorbed into the app the way a display permalink already is. | open.csr#36 | js-unit | site-demo.test.js | draft | ai-drafted | The document switcher and the explorer both link to reader pages. Absorbing them is what turns "leave the demo to read the synopsis" into "the synopsis is the second document in the viewer", with no renderer rewritten. |
| QC-DEMO-031 | Demo | A document selection resolves against the documents the page actually rendered, accepting either a document id or the basename of that document's reader page, and falls back to the first rendered document rather than to an empty pane. | open.csr#36 | js-unit | site-demo.test.js | draft | ai-drafted | The explorer names documents and a link between panes names pages; both mean the same document, so both resolve in one place rather than forcing every caller to know which vocabulary it holds. A stale deep link degrades to the primary document, exactly as a stale display slug degrades to the first display. |
| QC-DEMO-032 | Demo | The Documents pane renders one panel per assembled document from the same reader renderer, keyed by both document id and reader-page basename, shows exactly one, and hoists a single trace panel and index above the set rather than emitting one per document. | open.csr#36 | js-unit | site-demo.test.js | draft | ai-drafted | This is the Tables pane's arrangement applied to documents, deliberately: the reader already renders whichever document it is handed, so a pane is those fragments plus the rule that one is visible. `id="trace"` and `id="trace-index"` can each only mean one element, and a second copy of the trace script would answer every click twice. |
| QC-DEMO-033 | Demo | A document the page rendered is a selectable explorer node carrying its standalone permalink as the no-JavaScript fallback; a document the page did not render stays an ordinary link out with no selection attributes. | open.csr#36 | js-unit | site-demo.test.js | draft | ai-drafted | `is-elsewhere` was only ever a proxy for "the app holds one document". The real distinction is whether this page holds the document, so a standalone reader page keeps the link out while the app has none — and the node still works with JavaScript off, because the client upgrades a real link rather than replacing one. |
| QC-DEMO-034 | Demo | Every rendered document contributes its own top-level sections to the explorer, each section link naming its document, and a section slug is resolved inside the document on screen rather than across the page. | open.csr#36 | js-unit | site-demo.test.js | draft | ai-drafted | A section slug is a document's namespace, not the page's: the report and the synopsis both model `objectives` and `efficacy-results`, and they are different headings. Resolving by slug alone would send a deep link into the synopsis to a heading in the report. |
| QC-DEMO-035 | Demo | Each document panel carries its own prose disclosure, so a document whose blocks are unreviewed reads as draft inside the app exactly as it does on its own page. | open.csr#36 | js-unit | site-demo.test.js, site-documents.test.js | draft | ai-drafted | Rendering the synopsis in the main shell makes it look more finished, not less, so the disclosure matters more there. It travels with the document rather than being added to the shell, which is what makes a third draft template object disclosed for free. |
| QC-SITE-013 | Site | The display store states which documents place each display, built over the whole template library: the display's own page lists every document with a link to it and the number that document assigned, a display no document places says so, and the gallery card names those documents rather than counting them. | open.csr#42 | js-unit | site-render.test.js | draft | ai-drafted | The value store already answers "who cites this number?" and a display had no equivalent, so a reader could see a display but not where it appears. This is also the only surface where a display number is safe to print, because each number is stated beside the assembly that assigned it — which is what makes 14.3.1.2 and 13.4 read as two documents' names for one display rather than as a contradiction. |
| QC-SITE-014 | Site | A display placed in a document carries a hyperlink to its entry in the display store, written in the form the app's link-interception rule recognises, so the same href is a pane switch inside the demo and a real navigation on the standalone reader page. | open.csr#42 | js-unit | site-render.test.js | draft | ai-drafted | The other end of QC-SITE-013, and deliberately the same gesture a text block already offers to reach the Text Library: a trailing reference line under the object, naming it, linking to its own library page. A reader learns one gesture, not two — and writing the href the way `resolveAppLink` parses it is what keeps the demo from leaving itself to show a display it is already holding. |
| QC-SITE-015 | Site | A display's page lists the requirements that cover that display — the matrix rows matching the prefixes it declares in `site/config.json`, together with the ids its own evidence cites — and not the whole matrix. A display declaring no prefixes lists none rather than all of them. | Correctness | `display-requirements.test.js` | Verified |
| QC-SITE-012 | Site | The stylesheet expresses the explorer's one state and only that: a document's section list is hidden by default and revealed by sitting next to the current document, with no per-node collapse class and no disclosure-control rules left to override it. | open.csr#40 | js-unit | site-css.test.js | draft | ai-drafted | This half of the rule is CSS, not JavaScript, so removing the button from the markup would not by itself remove the second state — a leftover `.is-collapsed` rule would hide the open document's contents for anyone whose session still carried the flag. The markup tests cannot see that; this row can. |
| QC-SITE-011 | Site | Two documents assembled from the shared libraries quote the same artifacts: every display both documents place resolves to the same ARD source and the same ARD payload, and both carry the same value store with no value differing in raw value or formatted string. Only the per-document display number differs. | open.csr#36 | js-unit | site-documents.test.js | draft | ai-drafted | Rendering both documents in one viewer is where a disagreement would become visible to a reader. The reuse is real today and this row is the guard that keeps it real: a template that recomputed a number rather than citing a display would fail here before anyone read two different figures on the same screen. Display numbering is derived from assembly order per document (QC-DEMO-012), so it differs by design and is not evidence of divergence. |
| QC-SITE-008 | Site | `site/site.css` parses as a whole: every block is closed, no block is closed that was never opened, and no declaration sits outside a rule — so no rule in the file can be silently dead in the browser. | open.csr#16 | js-unit | site-css.test.js | draft | ai-drafted | CSS fails silently: one missing brace on 2026-07-27 killed every rule after line 2641 on `dev`, including the screen-reader-only class, so the sidebar's hidden labels rendered as visible text. Nothing else in the build reads the stylesheet — the site builder validates links and external resources, not structure. |
| QC-SITE-009 | Site | The site publishes one reader page and one template page for every assembled template object in `library/templates/`, discovered from the library rather than declared: an object absent from `site/config.json` still publishes, titled from its own model, and every document reaches every other from the switcher, the navigation tree and the home page. | open.csr#32 | js-unit | site-documents.test.js | draft | ai-drafted | The library went plural in #29 and the site did not follow, so the synopsis was built, committed, published on every deploy and reachable from no page. A site that names its documents needs editing to grow; a site that reads the library does not. The primary document keeps `/reader/` and `/templates/`, and every other document is a sibling file at the same depth so one set of relative links serves all of them. |
| QC-SITE-010 | Site | Every document states whether its own prose has been reviewed, derived from the `approval.state` the assembler recorded on each block it placed: a document holding unapproved blocks carries a notice above its first section and a flag in the switcher, the navigation tree and its home-page card; a document whose blocks are all approved says that instead. | open.csr#32 | js-unit | site-documents.test.js | draft | ai-drafted | The approval gate holds GENERATED-tier blocks only, so an unapproved boilerplate or parameterized block assembles into the document — "assembled" does not mean "reviewed", and the difference is invisible on the page unless it is stated there. The synopsis shipped with eighteen unreviewed blocks; a demo site presenting them as a finished document is exactly what a colleague reads and believes. Derived, never hand-set, so a third template object is disclosed the same way for free. |
| QC-DATA-001 | Data | The site reads a data registry (`site/config.json` → `data`) together with the vendored package's `PROVENANCE.json`: every registered dataset carries its upstream path, blob SHA-1, SHA-256 and byte counts and the package its pinned commit and licence; a registered dataset with no vendored file is planned, a derived or alternate-lane dataset is not reported as a missing file, and a vendored file nobody registered is published with a warning, the way an unregistered display is. A repository with no registry renders the empty state. | open.csr#76 | js-unit | site-data.test.js | draft | ai-drafted | The facts on the Data pages are the provenance record's, never typed; the registry adds only the title, the blurb and the derivation notes. |
| QC-DATA-002 | Data | The reverse index is built from the ARDs alone: a display whose current ARD names a dataset in its provenance envelope appears under that dataset with the iteration, row count, prepared hash and ARD hash the envelope recorded; a dataset no ARD names has an empty entry; a dataset an ARD names that the registry does not know still gets one. | open.csr#76 | js-unit | site-data.test.js | draft | ai-drafted | From a dataset to every number it produced — the link the user meeting asked for. |
| QC-DATA-003 | Data | The Data pane is one page with the package record, one section per dataset (facts, derivation notes, the read-by table linking each display in the form the app absorbs, the upstream path linking the source repository at the pinned commit, a derived dataset linking what it was derived from) and the lanes section naming both packagings and listing only the variables the agreement record says differ; each dataset also has a standalone page with the same facts. | open.csr#76 | js-unit | site-data.test.js | draft | ai-drafted | One page with ids, the Values arrangement, so a link lands by focus. |
| QC-DATA-004 | Data | Given the data index, a display's header links each dataset it declares to that dataset's page, carrying as its title the row count and hash THIS iteration's ARD recorded and a short hash beside the link; a dataset the header names but the ARD does not is still linked and says so; without the index the labels print as before. | open.csr#76 | js-unit | site-data.test.js | draft | ai-drafted | The label becomes the link; nothing else on the header moves. |
| QC-DATA-005 | Data | Data is a view of the study after Values and a collection in the explorer with one item per dataset (its id as the number, planned when not vendored) plus the lanes; a dataset permalink (`data/<id>.html`) is absorbed into the Data view with that dataset in focus, the lanes and the index likewise, at any authored depth. | open.csr#76 | js-unit | site-data.test.js, site-demo.test.js | draft | ai-drafted | Sixth tab; the link-rewriting rule learns one more directory. |
| QC-META-001 | Metadata | The study page renders `library/study.yaml` as declared: id, title, phase, cut-off and source, the arms in print order, the columns that carry an arm label with the assignment each means, and every analysis set with its label, flag and subjects per arm totalled — linking to the Data section for the source; a repository without a study model renders the empty state and reports it in the build warnings. | open.csr#77 | js-unit | site-metadata.test.js | draft | ai-drafted | Read through `study-lib`, the same validated loader the assembler's gate uses. |
| QC-META-002 | Metadata | The specifications page lists every display with its analysis and display files, its current iteration's spec, display and ARD hashes, and every iteration with the actor, change request and hashes its manifest recorded (either manifest shape); custom code shared through `custom_from` is stated from the borrowing side and the owning side; value declarations are read from `library/values/values.yaml` with their kind and declaration, each linking to the store. | open.csr#77 | js-unit | site-metadata.test.js | draft | ai-drafted | The history is the snapshots the pipeline already keeps beside every ARD. |
| QC-META-003 | Metadata | The approvals page lists every text block with its tier, version, approval state, approver, date and drafting model from its frontmatter, with counts of approved, draft and generated blocks held out of the report, each block linking to the Text view. | open.csr#77 | js-unit | site-metadata.test.js | draft | ai-drafted | The report's readiness on one page. |
| QC-META-004 | Metadata | Environments are every distinct R, operating-system and package-version record across every iteration of every display, each listing the iterations built in it; document models are the template objects on disk with sections, slots and the documents assembled from each, an empty library saying so; the requirements page lists every matrix under `quality/requirements/` with its row count, its source link and a route to its evidence page. | open.csr#77 | js-unit | site-metadata.test.js | draft | ai-drafted | Three more things that were recorded and never shown. |
| QC-META-005 | Metadata | Metadata is a view of the study after Data and a collection in the explorer with one item per page (the study id, the counts as numbers); the pane carries every section by id; a page permalink (`metadata/<page>.html`) is absorbed into the view with that section in focus and a fragment on it selects the deeper anchor; the Templates view keeps its own tab and permalink as the shortcut to the document models. | open.csr#77 | js-unit | site-metadata.test.js, site-demo.test.js | draft | ai-drafted | Seventh tab. |
| QC-LINK-001 | Links | Given the links, a display's header names the analysis set as the study model defines it (label and flag) and the grouping column, the specification history (iteration count), the environment (R version), the text blocks that bind it and the values sourced from it — each a link into the view that owns it; without the links the header prints as before; an empty list says so rather than printing nothing. | open.csr#78 | js-unit | site-links.test.js | draft | ai-drafted | Both directions from the display: what it was built against, what was built from it. |
| QC-LINK-002 | Links | The assembler records what a document was built from (`inputs`: the template object, the study model, every placed display with its iteration, ARD hash and datasets, every block with its state and derived inputs, every value cited, every dataset reached with the displays that read it) and, per block, the displays its bindings resolved against with iteration and hash; the Reader renders that record as a panel of links above the text and the provenance appendix as links to each display, each dataset and the environment. | open.csr#78 | js-unit | site-links.test.js, assemble-document.test.js | draft | ai-drafted | The one contract change of R3: derived at build, written into docs/assembled/*.json. |
| QC-LINK-003 | Links | A text block's card lists its inputs derived from its bindings — each display bound with the iteration and ARD hash resolved against, the values cited, its section and its approval, each a link — and names a declared display the block binds nothing from as drift; the frontmatter's `displays:` list stays the author's declaration, the build already failing a block that binds an undeclared display. | open.csr#78 | js-unit | site-links.test.js | draft | ai-drafted | Derived beside declared; neither replaces the other. |
| QC-LINK-004 | Links | The values pane links the arm a source address names to the study model, the datasets behind the value's display to the Data section, and the documents that place the blocks citing it; the arm link needs no index and is always present, the data and document links appear only when their indexes are given. | open.csr#78 | js-unit | site-links.test.js | draft | ai-drafted | A value's provenance reaches the data in two steps and the report in one. |
| QC-LINK-005 | Links | A document whose build failed a gate is flagged in the explorer (`gates fail`), one that passed is not, and one not yet assembled carries no verdict. | open.csr#78 | js-unit | site-links.test.js | draft | ai-drafted | A document that failed a gate is not a document. |
